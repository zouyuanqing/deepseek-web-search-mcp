[Unit]
Description=Restrict the SearXNG Docker port to the local LAN
After=docker.service
Wants=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/searxng-docker-firewall.sh
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
